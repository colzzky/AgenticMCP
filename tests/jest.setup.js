// This file is intended for global Jest setup configurations.
// It can be used to import 'jest-extended' for additional matchers,
// or to set up global mocks that are needed across multiple test files.
// For now, it remains a placeholder until specific global setup is required.

export {}; // Add a trivial export to make it a non-empty module
